import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    globalWrapper: {
        flex: 1,
        paddingLeft: 8,
        paddingRight: 8,
    },
});

export default styles;